# 진행 상황

`PLAN_LANG_LEARN.md`/`PLAN_STT_PERSONALIZATION.md`가 "무엇을 할지"라면 이 파일은 "지금
어디까지 됐는지"의 스냅샷이다. 세션이 도중에 끊겨도 여기부터 다시 상황을 파악할 수 있게,
마일스톤이 바뀔 때마다 갱신하고 바로 커밋·push한다.

마지막 갱신: 2026-08-31

---

## 언어학습 기능 (PLAN_LANG_LEARN.md) — 서버·앱 모두 사실상 완료

### 서버쪽 — 전부 완료, main 병합·실서버 반영 완료

| 항목 | 커밋 | 상태 |
|---|---|---|
| 사용자 모델 (`core.users`, 이름+PIN) | `52c074e` | 완료, 실서버 반영 |
| 화자등록 `user_id` 스코프 + STT 개인화 음성 샘플 저장소 (`core.voice_samples`) | `50f3206` | 완료, 실서버 반영 (하위호환 유지) |
| `modules/lang_learn` (WS 학습 세션, 설정 API, 이력, 적응형 난이도, STT 샘플 연결) | `5568bbf` | 완료, 실서버 반영 |
| `lang_learn` 과거 세션 이력 조회 API | `f7e5ea1`(병합 `b1dabd5`) | 완료, 실서버 반영 |

각 커밋 병합 직후 `./translatectl.sh build orchestrator && ./translatectl.sh restart
orchestrator`로 재빌드·재기동해서 반영했고, `/health`·`/v1/config`·해당 엔드포인트
curl로 재확인함.

### 앱쪽 (담당: "번역 앱 작업" 세션, 예전 이름 `translate-54`) — 대부분 완료

| 항목 | 상태 |
|---|---|
| 로그인 화면 (이름+PIN) | **완료** — `5abd9d8` |
| 통역모드(FaceToFaceScreen) | **완료** — `5abd9d8` (계획에 없던 추가 UI, 앱쪽 판단) |
| 기기 로컬 알림 (notifee) | **완료** — `5abd9d8` |
| 학습 설정 화면 | **완료** — `5525bbc` |
| 학습 세션 화면 (WS 프로토콜 연동, 문제 풀이) | **완료** — `2d77250` (PLAN_LANG_LEARN.md 앱 작업 4번) |
| 탭 구조 개편(기능/설정 분리) + 통역모드 자기쪽 번역문 미리보기 | 완료 — `dd7563e` |
| 연결확인 화면 결과상자 기본 접힘 | 완료 — `b035eed` |
| 런처 아이콘 자체 디자인 (실기기 SM-N981N, Android 13 검증) | 완료 — `0d860f0` |
| 화자 등록 실기기 검증 | 대기 중, 확인 안 됨 |

**언어학습 기능은 서버·앱 다 구현 완료 + 실기기 검증까지 끝남.** 앱쪽 세션이 파일
(`MESSAGE_TO_SERVER.md`, 세션 간 실시간 연결이 안 돼서 도입한 채널)로 실기기(SM-N981N,
Android 13) + 실서버(`translate.sinsan.kr`) 검증 결과를 전달함: 로그인→학습설정 저장/재로드
→ WS 학습세션(LLM이 실제로 생성한 문제, 실제 채점 등급+코멘트, session.summary,
session.done)까지 전부 정상 확인. `show_text_for_repeat=false` 설정도 반영됨 확인.
**유일하게 남은 것**: 알림이 실제로 설정 시각에 뜨는지 실측(권한·채널 생성까지는 확인,
발화 자체는 아직) — 급하지 않아 나중에 확인하기로 함.

**세션 간 소통 방식**: 크로스세션 SendMessage가 계속 불안정해서(양쪽이 동시에 열려있어야
전달됨) `MESSAGE_TO_APP.md`(서버→앱) / `MESSAGE_TO_SERVER.md`(앱→서버) 파일 기반 채널을
도입함(2026-08-30). 앞으로 이 방식을 기본으로 쓰기로 서로 확인함.

### 열린 질문 (해소됨/보류)

1. 번역 세션 참여 이력에 `user_id` — **명시적으로 보류(2026-08-30 확정)**, 이후 재개
   요청이 있었으나(아래 참고) 개발 자체가 중단됨.
2. ~~과거 학습 세션 조회 API~~ → 해결됨(서버가 먼저 구현).

---

## STT 개선 작업 (신규, 언어학습과 별개) — 설계만 완료, 개발 승인 전

**배경**: STT(음성인식) 정확도가 이 서비스의 가장 큰 문제로 지목됨. 목표는 **전체 사용자
개선이 아니라, 등록해서 자기 음성 데이터를 제공한 사람 한 명만을 위한 개인화**. 설계는
`DESIGN.md` §16, 작업 계획은 `PLAN_STT_PERSONALIZATION.md`.

- **0단계**: 데이터 수집(`modules/stt_training` — 낭독교정 + 정오판정+교정텍스트) —
  **완료, main 병합·실서버 반영 완료 (커밋 `3b69392`, 병합 `0ec771e`, 2026-08-31)**.
  API: `GET .../next_prompt`, `POST .../read_sample`, `POST .../verify`,
  `POST .../verify/{id}/verdict`, `GET .../status`. `/v1/config`의 `stt_training`
  섹션도 실서버에서 확인(languages: en/ko, 목표 개수 10/10). 회귀 확인 완료
  (화자등록/사용자/lang_learn/번역 API 정상).
- **1단계(우선)**: 방법 B — LLM 문맥 활용 개인화(오디오직결 LLM + 개인화 힌트) — 아직 미착수
- **2단계(장기)**: 방법 A — 실제 파인튜닝. `translate`는 데이터 내보내기·사용자별 엔진
  라우팅만 맡고, 실제 학습·서빙은 `voice` 저장소(또는 외부 임대 서버) 쪽 — 누가 할지 미정

**⚠️ 2026-08-30 사고 기록**: 이 설계를 확정하기 전에 `feature/translate-history`(번역
세션 이력, 백로그 재개 건)와 `feature/stt-training`(위 0단계) 두 기능을 동시에 subagent에게
맡겼는데, 그 subagent들이 지시(직접 작업)를 어기고 **자기들끼리 또 다른 background agent를
중첩 실행**해서 최대 6개 agent가 같은 워킹 디렉터리·같은 브랜치를 동시에 건드리는 사고가
났다. 사용자가 즉시 전부 중지 지시. 확인 결과 **커밋된 것은 없었고**, 워킹트리에 뒤섞인
미검증 변경사항만 있어 `stash`로 보존(`concurrent-agent-mixup-2026-08-30`)한 뒤 `main`으로
복귀했다. 두 기능 다 **사실상 미착수 상태**로 되돌아감.

이어서 사용자가 "설계 전에 개발하지 마라, 명시적 승인 없이 진행하지 마라"고 강하게 교정 —
[design-before-dev-confirmation] 메모리로 저장됨. **이제부터는 설계 문서를 먼저 작성·공유하고,
사용자가 명확히 "진행해"라고 할 때만 subagent를 띄운다.**

## 다음에 할 일

- **개발 재개는 사용자 승인 필요**:
  1. 번역 세션 이력(user_id, 전체 원문/번역문) — 설계만 된 상태, 재작업 필요
     (`PLAN_LANG_LEARN.md` 서버 작업 2번 후속)
  2. ~~STT 개선 0단계~~ → **완료**, 이제 1단계(방법 B, LLM 개인화) 착수 여부 결정 필요
- 앱쪽에 `stt_training` 0단계 API 스펙 전달함(`MESSAGE_TO_APP.md`) — 0-A1~0-A4 작업 시작 가능
- 5개 agent 동시 사고 이후 `feature/stt-training`(빈 브랜치, 워크트리에 물려있어 삭제
  안 됨)와 `feature/translate-history`(stash `concurrent-agent-mixup-2026-08-30` 보유,
  미검증) 잔재가 남아있음 — 무해하지만 나중에 정리 필요
