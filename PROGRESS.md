# 진행 상황

`PLAN_LANG_LEARN.md`가 "무엇을 할지"라면 이 파일은 "지금 어디까지 됐는지"의 스냅샷이다.
세션이 도중에 끊겨도 여기부터 다시 상황을 파악할 수 있게, 마일스톤이 바뀔 때마다
갱신하고 바로 커밋·push한다.

마지막 갱신: 2026-08-30

---

## 서버쪽 (orchestrator/) — 전부 완료, main 병합·실서버 반영 완료

| 항목 | 커밋 | 상태 |
|---|---|---|
| 사용자 모델 (`core.users`, 이름+PIN) | `52c074e` | 완료, 실서버 반영 |
| 화자등록 `user_id` 스코프 + STT 개인화 음성 샘플 저장소 (`core.voice_samples`) | `50f3206` | 완료, 실서버 반영 (하위호환 유지) |
| `modules/lang_learn` (WS 학습 세션, 설정 API, 이력, 적응형 난이도, STT 샘플 연결) | `5568bbf` | 완료, 실서버 반영 |
| `lang_learn` 과거 세션 이력 조회 API (`GET /v1/users/{id}/lang_learn/history`) | `f7e5ea1` (병합 커밋 `b1dabd5`) | 완료, 실서버 반영 |

각 커밋 병합 직후 `./translatectl.sh build orchestrator && ./translatectl.sh restart
orchestrator`로 재빌드·재기동해서 반영했고, `/health`·`/v1/config`·해당 엔드포인트
curl로 재확인함.

## 앱쪽 (app/, 담당: "번역 앱 작업" 세션 — 예전 이름 `translate-54`) — 진행 중

| 항목 | 상태 |
|---|---|
| 로그인 화면 (이름+PIN, `LoginScreen.tsx`) | **완료** — 커밋 `5abd9d8` (2026-08-30), main에 직접 push됨 |
| 통역모드 (`FaceToFaceScreen.tsx`, 화면 위/아래 분할) | **완료** — 같은 커밋. 계획서에 없던 추가 UI지만 앱쪽 판단으로 포함된 것으로 보임 |
| 기기 로컬 알림 (notifee, Android 13+ 권한·부팅 후 재예약 포함) | **완료** — 같은 커밋 |
| 학습 세션 화면 (WS 프로토콜 연동) | 최종 확정 스펙 전달함(2026-08-26). 이번 커밋 메시지에 명시적 언급 없음 — 포함 여부 재확인 필요 |
| 화자 등록 실기기 검증 | 대기 중, 확인 안 됨 |

`5abd9d8` 커밋 메시지: "관련 테스트(login, facetoface, notifications, screens) 전부
통과." `app.config.json`의 `serverUrl`은 로컬 테스트값이라 커밋에서 제외했다고 명시.

**참고**: 이 앱쪽 세션과 지금 크로스세션 메시지가 연결되지 않는 상태(`ListAgents`에
안 잡힘)라, 위 내용은 git 커밋 로그로만 확인한 것이고 직접 확인·질문은 못 했다.

## 열린 질문 (미결정)

1. **번역 세션 참여 이력에 `user_id`를 붙일지** — PLAN_LANG_LEARN.md 서버 작업 2번에서
   보류한 항목. 지금 기능에 필수는 아니라 결정을 미룬 상태.
2. ~~과거 학습 세션 조회 API 필요 여부~~ → **해결됨.** 앱팀 답변을 못 받아 서버쪽에서
   먼저 구현해서 준비해둠(`f7e5ea1`). 앱이 실제로 이 화면을 만들지는 앱쪽 판단.

## 다음에 할 일

- 앱쪽 세션과 연결이 복구되면: 학습 세션 화면(WS 연동) 포함 여부 확인, 실기기 검증
  결과 확인, 이력 조회 API를 쓸지 확인
- 앱쪽 구현이 실기기까지 검증되면, DESIGN.md §15 1단계 전체가 "사업성 검증 가능한
  MVP"로 완성됨 — 그다음이 2단계(다중 테넌트) 착수 여부 판단
