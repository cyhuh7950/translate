# 진행 상황

`PLAN_LANG_LEARN.md`가 "무엇을 할지"라면 이 파일은 "지금 어디까지 됐는지"의 스냅샷이다.
세션이 도중에 끊겨도 여기부터 다시 상황을 파악할 수 있게, 마일스톤이 바뀔 때마다
갱신하고 바로 커밋·push한다.

마지막 갱신: 2026-08-26

---

## 서버쪽 (orchestrator/) — 전부 완료, main 병합·실서버 반영 완료

| 항목 | 커밋 | 상태 |
|---|---|---|
| 사용자 모델 (`core.users`, 이름+PIN) | `52c074e` | 완료, 실서버 반영 |
| 화자등록 `user_id` 스코프 + STT 개인화 음성 샘플 저장소 (`core.voice_samples`) | `50f3206` | 완료, 실서버 반영 (하위호환 유지) |
| `modules/lang_learn` (WS 학습 세션, 설정 API, 이력, 적응형 난이도, STT 샘플 연결) | `5568bbf` | 완료, 실서버 반영 |

세 커밋 모두 `main`에 fast-forward 병합, `origin/main`에 push 완료. 실서버
(`translate.sinsan.kr`)는 각 병합 직후 `./translatectl.sh build orchestrator &&
./translatectl.sh restart orchestrator`로 재빌드·재기동해서 반영했고, `/health`·
`/v1/config`·해당 엔드포인트 curl로 재확인함.

## 앱쪽 (app/, 담당: `translate-54`) — 스펙 전달 완료, 구현/검증 상태 미확인

| 항목 | 상태 |
|---|---|
| 로그인 화면 (이름+PIN) | 스펙 전달함, 진행 여부 미확인 |
| 학습 설정 화면 | 스펙 전달함, 진행 여부 미확인 |
| 기기 로컬 알림 스케줄링 | 지시함(서버 FCM 푸시 없음), 진행 여부 미확인 |
| 학습 세션 화면 (WS 프로토콜 연동) | 최종 확정 스펙 전달함(2026-08-26), 실기기 검증 여부 미확인 |
| 화자 등록 실기기 검증 (`feature/speaker-enrollment` — 이미 병합 전 별도 이력) | 대기 중, 확인 안 됨 |

## 열린 질문 (미결정, 앱쪽 답변 대기 중)

1. **번역 세션 참여 이력에 `user_id`를 붙일지** — PLAN_LANG_LEARN.md 서버 작업 2번에서
   보류한 항목. 지금 기능에 필수는 아니라 결정을 미룬 상태.
2. **과거 학습 세션 조회 API 필요 여부** — `GET /v1/users/{id}/lang_learn/history` 같은
   엔드포인트가 필요한지 앱쪽에 물어봤고 아직 응답 없음. 필요하다고 하면 서버쪽에
   신규 엔드포인트 하나 추가하면 됨(저장소는 이미 있음, `LangLearnHistoryStore`).

## 다음에 할 일

- `translate-54` 응답 확인 → 위 열린 질문 결정 → 필요하면 이력 조회 엔드포인트 추가
- 앱쪽 구현이 실기기까지 검증되면, DESIGN.md §15 1단계 전체가 "사업성 검증 가능한
  MVP"로 완성됨 — 그다음이 2단계(다중 테넌트) 착수 여부 판단
