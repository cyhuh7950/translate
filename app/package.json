{
  "name": "translate-app-api",
  "version": "0.1.0",
  "private": true,
  "description": "Protocol/API layer for the translate mobile app (framework-free TypeScript)",
  "license": "UNLICENSED",
  "main": "src/api/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "typecheck:test": "tsc --noEmit -p tsconfig.test.json",
    "build:test": "tsc -p tsconfig.test.json",
    "smoke": "npm run build:test && node dist/test/smoke.js"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.9.0"
  }
}
