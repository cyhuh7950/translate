# 음성 번역 프로그램 설계

STT → LLM → TTS 파이프라인으로 음성을 번역해 다시 음성으로 돌려주는 시스템.

**설계 원칙** — 기존 엔진 레이어에서 이어간다.

> **아무것도 고정하지 않는다. 바꿀 수 있는 건 전부 설정에서 바꾼다.**
>
> 코드에는 특정 서버·도메인·포트·엔진·프로바이더·노출 방식이 등장하지 않는다.
> 판단 기준은 "이걸 나중에 다르게 하고 싶어질 수 있는가"이고, 답이 예이면 설정으로 뺀다.
> 하드코딩된 기본값이 필요하면 **설정 파일의 기본값**으로 두지 코드에 두지 않는다.

- 모든 계층이 교체 가능하다. STT·LLM·TTS·클라이언트 어느 것도 특정 구현에 묶이지 않는다
- 오케스트레이터와 엔진은 각각 독립 컨테이너다. 필요한 것만 골라 띄운다
- 엔진에 닿는 방법(프록시·서브도메인·직접 포트·사설망)을 시스템이 가정하지 않는다
- 설정 단일 소스는 `<컴포넌트>/*.env` 와 레지스트리. `voicectl.sh` 가 폴더를 자동 탐색한다
- 새 엔진·프로바이더 추가는 설정 추가로 끝난다. 재빌드·재기동이 필요 없어야 한다

### 소스에 남는 것과 설정으로 나가는 것

| | 내용 |
|---|---|
| **소스에 남는다** | "어떻게 하는가"의 구현뿐. 파이프라인 로직, 프로토콜 처리, 각 어댑터의 본체 |
| **소스에 없다** | 모든 "무엇을·어디에·얼마나". 이름, 주소, 포트, 개수, 임계값, 목록, 순서, 우선순위, 기본값 |

**기본값도 소스에 두지 않는다.** `os.getenv("PORT", "8101")` 처럼 코드에 박힌 폴백은 쓰지 않는다.
기본값은 배포에 포함되는 **기본 설정 파일**에 두고, 코드는 설정이 없으면 명확한 오류로 죽는다.
그래야 "지금 이 값이 어디서 왔는지"가 항상 설정 파일 하나로 추적된다.

**구현체 선택이 분기문에 있으면 안 된다.** 이게 이 원칙의 실질적인 기준선이다.

```python
# 이렇게 쓰지 않는다 — 새 구현을 넣을 때마다 소스를 고쳐야 한다
if backend == "silero":   return SileroVAD()
elif backend == "webrtc": return WebRtcVAD()

# 레지스트리에 등록하고 설정이 이름으로 고른다. 소스 수정 없이 구현이 늘어난다
vad = registry.resolve("vad", config.vad.backend, **config.vad.options)
```

VAD, STT/TTS 어댑터, LLM 어댑터 계열, 라우팅 정책, 턴 정책, 문장 경계 판정 — 전부 같은 방식이다.
새 구현을 추가하는 일은 **파일 하나 추가 + 설정에서 이름 지정**으로 끝나야 하고,
기존 파일을 여는 순간 그 설계는 실패한 것이다.

**모드·정책 이름도 열려 있다.** `realtime`/`ptt`/`batch`, `half_duplex`/`barge_in`/`full_duplex` 는
기본 제공 구현의 이름일 뿐 고정된 열거형이 아니다. 같은 레지스트리에 새 이름을 등록할 수 있다.

**이 문서에 나오는 모든 숫자와 이름은 예시다.** 포트 `8401`, 도메인 `sinsan.kr`, 모델 `base`,
임계값 `500ms` — 어느 것도 규격이 아니다. 실제 값은 전부 설정에서 온다.

---

## 1. 전체 구조

```
   클라이언트                    오케스트레이터                    엔진 / 프로바이더
┌──────────────┐          ┌────────────────────────┐
│ 웹 브라우저    │  WS      │  voice-orchestrator     │   HTTP   ┌──────────────┐
│ (마이크/재생)  ├─────────▶│  :8401                  ├─────────▶│ STT  81xx    │
├──────────────┤          │                         │          │ whisper      │
│ CLI / 외부 앱 │  HTTP    │  ┌───────────────────┐  │          │ whisper-stream│
│              ├─────────▶│  │ 세션·턴 관리        │  │          │ moonshine    │
├──────────────┤          │  │ VAD / 세그먼트 결정 │  │          └──────────────┘
│ 데스크톱/모바일│  WS      │  │ 파이프라인 코어     │  │   HTTP   ┌──────────────┐
│              ├─────────▶│  └───────────────────┘  ├─────────▶│ TTS  82xx    │
└──────────────┘          │                         │          │ supertonic   │
                          │  ┌───────────────────┐  │          │ cosyvoice3   │
                          │  │ STT 어댑터         │  │          │ qwen3-tts    │
                          │  │ LLM 어댑터 (9종)   │  │          └──────────────┘
                          │  │ TTS 어댑터         │  │   HTTPS  ┌──────────────┐
                          │  └───────────────────┘  ├─────────▶│ LLM 프로바이더 │
                          └────────────────────────┘          │ 원격 8종      │
                                                               │ + ollama 8301│
                                                               └──────────────┘
```

### 포트 — 관례일 뿐 규칙이 아니다

사람이 기억하기 좋으라고 쓰는 관례다. **시스템은 포트로 종류를 추론하지 않는다.**
종류는 `ENGINE_KIND` 메타데이터로만 판단하므로, 서버 사정에 따라 아무 포트나 써도 된다.

| 대역 | 관례상 용도 |
|---|---|
| `81xx` | STT 엔진 |
| `82xx` | TTS 엔진 |
| `83xx` | 로컬 LLM |
| `84xx` | 오케스트레이터 / 웹 클라이언트 |

포트는 각 컴포넌트의 `*.env` 에서 정하고, 오케스트레이터는 레지스트리의 URL 만 본다.
`voicectl.sh` 는 이미 폴더를 자동 탐색하므로 `ENGINE_KIND` 에 `llm`, `orchestrator`, `web` 을
추가하면 새 컴포넌트도 같은 명령으로 관리된다.

---

## 2. 세 가지 모드

세 모드는 **같은 파이프라인 코어**를 쓴다. 다른 것은 "입력을 어디서 받고, 언제 한 덩어리로 확정하는가"뿐이다.

| 모드 | 프로토콜 | 세그먼트 확정 | 지연 목표 | 용도 |
|---|---|---|---|---|
| `realtime` | WebSocket | VAD 자동 | TTFA 1~2초 | 대화형 통역 (**주 목표**) |
| `ptt` | WebSocket / HTTP | 버튼 뗄 때 | 3~5초 | 소음 환경, 안정성 우선 |
| `batch` | HTTP | 파일 전체 | 제약 없음 | 녹음·영상 번역, 자막 |

모드는 세션 설정으로 고른다. 서버 기본값은 `orchestrator.env` 의 `DEFAULT_MODE`.

**모드별 요구 사항**

- `realtime` 은 스트리밍 STT 와 스트리밍 TTS 가 있어야 성립한다. 배치 전용 엔진만 있으면
  오케스트레이터가 거부하고 그 이유를 응답에 담는다 (조용히 ptt 로 격하시키지 않는다)
- `ptt`, `batch` 는 지금 오라클 CPU 스택으로도 동작한다

---

## 3. 실시간 파이프라인 (핵심)

체감 지연을 결정하는 건 전체 처리 시간이 아니라 **첫 소리까지의 시간(TTFA, time-to-first-audio)**
이다. 그래서 각 단계가 앞 단계의 완료를 기다리지 않고 점진적으로 흘려보내는 게 설계의 전부다.

```
마이크 ──20ms PCM 프레임──▶ VAD ──발화 구간──▶ 스트리밍 STT
                                                    │
                                    partial(중간)    │ final(확정)
                                    화면에만 표시 ◀──┤
                                                    ▼
                                            문장 경계 판단
                                                    │ 문장 단위
                                                    ▼
                                          LLM 번역 (토큰 스트리밍)
                                                    │ 절/문장 단위로 잘라
                                                    ▼
                                            TTS 청크 합성
                                                    │
                                                    ▼
                                       클라이언트 오디오 큐 → 재생
```

**단계별 설계 포인트**

1. **VAD** — 발화 시작/끝 감지. 구현체도 교체 가능해야 하므로 어댑터로 두고
   설정에서 고른다 (`VAD_BACKEND` — silero / webrtc / 에너지 임계값 / 외부 서비스 등).
   끝 판정 무음 임계값이 지연과 오검출의 트레이드오프 핵심이다.
   300ms 면 빠르지만 말 중간에 끊기고, 800ms 면 안전하지만 느리다. 전부 설정값으로 노출한다
   (`VAD_SILENCE_MS`, `VAD_THRESHOLD`, `VAD_MIN_SPEECH_MS`)

2. **스트리밍 STT** — partial 은 화면 표시용으로만 쓰고 **번역에 넣지 않는다.**
   partial 은 계속 뒤집히기 때문에, 번역하면 음성 출력이 오락가락한다.
   확정(final)된 문장만 LLM 으로 넘긴다

3. **문장 경계** — 발화 전체를 기다리지 않고 문장이 끝날 때마다 번역을 시작한다.
   종결어미·문장부호·VAD 짧은 휴지를 함께 본다. 한국어는 문장부호가 안 나오는 경우가 많아
   종결어미 패턴과 휴지 길이가 주 신호가 된다

4. **LLM 스트리밍** — 토큰을 받으면서 절 단위로 TTS 에 밀어넣는다.
   번역은 어순이 바뀌므로(한→영) 너무 잘게 자르면 어색해진다.
   최소 청크 길이(`MIN_TTS_CHARS`)를 두고 그 이상 모이면 내보낸다

5. **TTS 청크** — 문장/절 단위로 합성해 즉시 전송. CosyVoice3 의 양방향 스트리밍을 쓰면 더 줄어든다

6. **재생** — 클라이언트가 오디오 큐에 이어붙여 끊김 없이 재생. seq 번호로 순서를 보장한다

### 참여자와 번역 방향 — 단방향은 양방향의 특수 케이스

최종 목표는 양방향 통역이다. 단방향을 먼저 만들더라도 **모델은 처음부터 양방향으로 둔다.**
그래야 확장이 재작성이 아니라 설정 변경이 된다.

세션은 **참여자 목록**을 갖는다. 참여자 하나는 (언어, 입력 여부, 출력 대상)이다.

```yaml
session:
  participants:
    - { id: a, lang: ko, input: true, output: [b] }
    - { id: b, lang: en, input: true, output: [a] }
```

번역 방향은 **발화자의 언어 → 수신자의 언어**로 그때그때 정해진다. 코드에 "한→영"이 없다.

| 구성 | 설정 |
|---|---|
| 양방향 | 참여자 둘 다 `input: true` |
| 단방향 | 한쪽을 `input: false` — 코드 경로는 완전히 동일하다 |
| 1:N 통역 | `output` 에 여러 명. 모델이 막지 않는다 (지금 필요 없어도) |

**즉 단방향 구현은 양방향 코드에 참여자 설정만 다르게 준 것이다.** 별도 분기가 생기지 않는다.

#### 세션 프로필 — 단방향/양방향을 이름으로 고른다

참여자 목록을 매번 손으로 쓰게 하지 않는다. 자주 쓰는 구성을 **프로필로 묶어 설정 파일에 둔다.**
클라이언트는 이름만 고르면 된다.

```yaml
# 설정 파일. 코드가 아니다.
session_profiles:
  oneway:
    label: "단방향 (한 사람 발화)"
    participants:
      - { id: speaker,   lang: "${source_lang}", input: true,  output: [listener] }
      - { id: listener,  lang: "${target_lang}", input: false }
    speaker_id: manual

  twoway:
    label: "양방향 (대화 통역)"
    participants:
      - { id: a, lang: "${source_lang}", input: true, output: [b] }
      - { id: b, lang: "${target_lang}", input: true, output: [a] }
    speaker_id: language_detect
    turn_policy: half_duplex
```

- **`oneway`/`twoway` 는 코드에 없다.** 설정 파일의 키일 뿐이라 이름을 바꾸거나
  프로필을 더 만들 수 있다 (`presentation`, `interview`, `three_way` 등)
- 언어는 프로필이 고정하지 않고 세션이 넘긴 값을 채운다
- 프로필을 쓰지 않고 참여자 목록을 직접 넘겨도 된다. 프로필은 편의 기능이다
- `GET /v1/config` 가 **사용 가능한 프로필 목록과 label 을 돌려주고**, 클라이언트는 그걸로
  선택 UI 를 그린다. 목록을 클라이언트에 하드코딩하지 않는다

프로필이 요구하는 어댑터(예: `twoway` 의 `language_detect`)가 등록돼 있지 않으면
그 프로필은 `/v1/config` 응답에서 **사용 불가로 표시된다.** 엔진 가용성과 같은 방식이다.
1단계에서는 `twoway` 가 이 이유로 비활성이고, 2단계에서 어댑터를 등록하면 그대로 켜진다.

#### 화자 식별 방식 (설정)

"누가 말했는지"를 어떻게 아는가도 고정하지 않는다.

| 방식 | 동작 | 적합한 상황 |
|---|---|---|
| `channel` | 참여자마다 별도 연결. 누가 말했는지 자명 | 각자 기기를 쓸 때 |
| `language_detect` | 한 채널에 섞여 들어오고 감지된 언어로 판별 | 한 기기를 공유할 때 |
| `manual` | 클라이언트가 명시 (참여자 선택 버튼) | PTT |
| `diarization` | 화자 분리 모델 | 회의 녹음 배치 |

#### 토폴로지 두 가지

**단일 기기** — 한 마이크·스피커를 두 사람이 공유한다. 화자 식별이 `language_detect` 나 `manual`
이어야 하고, 번역 음성이 같은 스피커로 나가므로 `half_duplex` 가 자연스럽다.
클라이언트가 하나라 구현이 단순하고, 단방향에서 넘어오는 경로가 가장 짧다.

**다중 기기** — 참여자마다 클라이언트가 붙어 세션 ID 로 합류한다. 참여자별 언어가 고정이라
감지가 필요 없고, 오디오가 물리적으로 분리돼 `full_duplex` 가 가능하다. 대신 세션 공유·동기화
인프라가 필요하고, 운영에서는 WebSocket 세션 어피니티 문제와 직결된다.

둘 다 지원하되 설정으로 고른다. 확장 경로상 **단일 기기가 먼저**다.

### 턴 관리 (설계상 가장 까다로운 부분)

실시간 양방향 통역에서는 **번역 음성이 나가는 중에 사용자가 계속 말한다.** 그대로 두면
스피커 소리가 마이크로 들어가 자기 번역을 다시 번역하는 루프가 생긴다.

세 가지 정책을 설정으로 고를 수 있게 한다.

| 정책 | 동작 | 적합한 상황 |
|---|---|---|
| `half_duplex` | TTS 재생 중 STT 입력 무시 | 헤드셋 없는 환경. 가장 안전 |
| `barge_in` | 사용자 발화 감지 시 TTS 즉시 중단 | 헤드셋 착용. 자연스러움 |
| `full_duplex` | 동시 처리 | AEC(에코 제거)가 있는 클라이언트만 |

기본값은 `half_duplex`. 브라우저는 `echoCancellation: true` 를 켜되, 그것만 믿지 않는다.

---

## 4. 지연 예산

### 실시간 모드 (GPU 서버 기준, 목표치)

| 단계 | 예상 |
|---|---|
| VAD 발화 끝 판정 | 300~500ms |
| 스트리밍 STT 확정 | 500~800ms |
| LLM 첫 토큰 | 200~500ms |
| TTS 첫 청크 | 300~800ms |
| **TTFA 합계** | **1.3~2.6초** |

측정 전 추정치다. 구현 후 각 단계 실측을 `X-*` 헤더와 WS 이벤트 타임스탬프로 남겨 갱신한다.

**LLM 프로바이더 선택이 여기서 의미를 갖는다.** Groq·Cerebras 는 첫 토큰 지연이 매우 짧아
실시간에 유리하고, 품질이 필요한 배치에는 Anthropic·OpenAI 가 낫다. 그래서 프로바이더를
모드별로 다르게 지정할 수 있게 한다.

### 오라클 CPU 스택 실측 (배치/PTT 기준선)

| 단계 | 실측 |
|---|---|
| STT whisper base/int8 | 3.7s (RTF 1.12) |
| LLM 번역 | 0.5~2s (프로바이더별) |
| TTS supertonic | 6.4s (RTF 1.93) |
| **합계** | **약 11~12초** |

→ 이 하드웨어에서는 PTT·배치만 쓸 만하다. **이건 개발용 ARM CPU 박스의 한계지 시스템의 성질이 아니다.**
운영 서버에서 같은 whisper 가 RTF 0.1 로 돌면 그대로 실시간에 투입된다.
그래서 모드 가능 여부를 코드에 박지 않고 엔진 레지스트리의 `modes` 메타데이터로 다룬다 (9장 참고).

---

## 5. LLM 프로바이더 추상화

9종을 3개 계열로 덮는다.

| 계열 | 프로바이더 | 비고 |
|---|---|---|
| OpenAI 호환 | Cerebras, Groq, Mistral, OpenRouter, Upstage, OpenAI, Ollama | `base_url` 만 다르다. 어댑터 1개로 처리 |
| Anthropic 네이티브 | Anthropic | Messages API |
| Gemini 네이티브 | Gemini | OpenAI 호환 엔드포인트도 있어 1계열로 처리 가능. 네이티브가 기능이 더 많다 |

**프로바이더 목록도 코드에 없다.** 위 9종은 기본 제공 설정일 뿐이고, 설정 파일에서 추가·제거된다.
어댑터 계열(`KIND`)만 코드에 있고, OpenAI 호환이면 새 프로바이더는 설정만으로 붙는다.
호환되지 않는 API 가 나오면 그때 계열 하나를 추가한다 — 이것이 유일하게 코드가 필요한 확장이다.

**어댑터 인터페이스**

```python
async def translate_stream(
    text: str, *, source_lang: str, target_lang: str,
    context: list[Turn], glossary: dict[str, str],
) -> AsyncIterator[str]:      # 토큰 델타를 순서대로
```

프로바이더별 설정은 엔진과 같은 방식으로 `providers/<이름>.env` 에 둔다.

```
# providers/groq.env
PROVIDER=groq
KIND=openai_compatible
BASE_URL=https://api.groq.com/openai/v1
API_KEY=
DEFAULT_MODEL=
```

새 프로바이더 추가 = `.env` 파일 하나 추가. 코드 수정 없음(OpenAI 호환인 경우).

### 번역 품질 설계

- **역할 프롬프트** — 통역사 역할, 번역문만 출력, 설명·주석·따옴표 금지
- **대화 맥락** — 최근 N턴을 컨텍스트로 전달. 한국어는 주어 생략이 많아 맥락 없이는 대명사를 틀린다
- **용어집** — 고유명사·전문용어를 세션별로 주입해 일관성 확보
- **원문 보존** — 의역 금지 수준을 설정으로 (`literal` / `natural`)
- **실패 처리** — 프로바이더 오류 시 대체 프로바이더로 폴백(설정 가능). 실시간에서는
  폴백 지연이 더 나쁠 수 있으므로 기본은 폴백 없이 오류 전달

---

## 6. 설정 체계

우선순위: **요청 파라미터 > 세션 설정 > 서버 기본값**

```jsonc
// 세션 설정 (WS config 메시지 또는 HTTP 파라미터)
{
  "mode": "realtime",
  "source_lang": "ko",
  "target_lang": "en",
  "stt":  { "engine": "whisper-stream", "url": "http://voice-whisper-stream:8103" },
  "llm":  { "provider": "groq", "model": "...", "style": "natural" },
  "tts":  { "engine": "cosyvoice3", "url": "http://voice-cosyvoice3:8203", "voice": "F1" },
  "turn": { "policy": "half_duplex", "vad_silence_ms": 500 }
}
```

엔진을 **URL 로 지정**하는 게 배포 유연성의 핵심이다. 엔진이 같은 서버에 있든 다른 서버에 있든
오케스트레이터는 신경 쓰지 않는다.

`GET /v1/config` 로 현재 사용 가능한 엔진·프로바이더 목록과 각각의 지원 모드를 돌려준다.
클라이언트는 이걸로 설정 UI 를 그린다 (목록을 하드코딩하지 않는다).

### 다국어 — 언어에 묶이지 않는다

**축이 두 개다. 섞으면 안 된다.**

| 축 | 무엇 | 누가 정하나 |
|---|---|---|
| 번역 언어 | `source_lang` / `target_lang` | 세션. 코드에 언어쌍 상수가 없다 |
| 표시 언어 | `locale` — 화면에 보이는 문구 | 클라이언트 |

**기본어는 영어다.** 한국어가 아니다. 지역화되지 않은 문구는 전부 영어로 떨어진다.

설정 파일의 사람이 읽는 문구는 문자열 하나로 써도 되고 로케일 맵으로 써도 된다.

```yaml
label: "One-way"          # 모든 로케일에서 이 값
label:                    # 로케일별. en 이 폴백이다.
  en: "One-way — one person speaks, the other listens"
  ko: "단방향 — 한 사람이 말하고 상대 언어로 출력"
```

로케일 결정 순서: **`?locale=` 쿼리 > `Accept-Language` 헤더 > `en`.**
요청한 로케일에 번역이 없으면 조용히 영어로 떨어지고, 응답에는 요청받은 로케일을 그대로 돌려준다.

**남은 과제 — 최종 목표는 모든 문구를 사용자 언어로 내보내는 것이다.**
지금은 설정의 label/description 만 지역화되고 오류 메시지는 영어 고정이다.
완전 지역화하려면 오류에 **메시지 코드와 파라미터**를 붙여 클라이언트가 자기 카탈로그로
문장을 만들게 해야 한다.

```jsonc
// 지금
{ "detail": "Unknown engine: 'x'. Available: whisper, moonshine" }
// 목표
{ "code": "engine.unknown", "params": { "id": "x", "available": ["whisper", "moonshine"] },
  "message": "Unknown engine: 'x'. ..." }   // message 는 영어 폴백
```

`message` 를 남겨두면 카탈로그가 없는 클라이언트도 동작한다. 웹 클라이언트(1단계 ④)는
자체 로케일 파일을 갖고, 그때 이 코드 체계를 함께 도입한다.

### 설정 가능 항목 (구현 시 체크리스트)

"아무것도 고정하지 않는다"를 검증 가능하게 나열한다. 여기 있는 것 중 코드에 박힌 게 있으면 버그다.

| 영역 | 설정 가능한 것 |
|---|---|
| **엔진** | 목록, 종류, 엔드포인트 URL(개수 제한 없음), 선택 조건·우선순위, API 키, 지원 모드, 스트리밍 여부, 타임아웃, 재시도 |
| **노출 방식** | 프록시·서브도메인·직접 포트·사설망 — URL 로만 표현되므로 시스템은 구분하지 않음 |
| **LLM** | 프로바이더 목록, base_url, 모델, 어댑터 계열, 온도 등 생성 파라미터, 모드별 다른 프로바이더, 폴백 여부와 순서 |
| **번역 품질** | 시스템 프롬프트, 번역 스타일(literal/natural), 맥락 턴 수, 용어집 |
| **VAD** | 백엔드 구현체, 무음 임계값, 최소 발화 길이, 감지 민감도 |
| **세그먼트** | 문장 경계 판정 방식, 최소 TTS 청크 길이, 강제 확정 시간 |
| **턴 관리** | half_duplex / barge_in / full_duplex, 전환 조건 |
| **모드** | 기본 모드, 모드별 허용 엔진, 세션이 모드를 바꿀 수 있는지 |
| **세션 구성** | 세션 프로필 목록(단방향·양방향 등), 참여자 수와 언어, 화자 식별 방식, 프로필 없이 직접 지정 |
| **라우팅** | 정책(explicit / mode_first / nearest / fallback), 후보 우선순위 |
| **오디오** | 입력 샘플레이트·프레임 크기, 출력 포맷·샘플레이트 |
| **인증** | API 키, 인증 방식, `/health` 공개 여부, CORS 허용 출처 |
| **배포** | 컴포넌트 포트, 바인딩 주소, 서버 식별자, 설정 프로파일(dev/prod) |
| **한계** | 최대 세션 수, 최대 입력 길이, 큐 깊이, 백프레셔 정책 |

**언어도 고정하지 않는다.** 지원 언어 목록은 엔진 `/info` 응답에서 가져오고, 번역 언어쌍은
세션이 정한다. 한↔영을 코드가 특별 취급하지 않는다.

---

## 7. 엔진 레지스트리 — 두 서버의 엔진을 API 로 통합

오케스트레이터는 **엔진을 자기 안에 품지 않는다.** 전부 원격 HTTP 엔드포인트로만 다룬다.
같은 서버에 있든 다른 서버에 있든 코드는 동일하고, 차이는 URL 과 네트워크 지연뿐이다.

이 덕분에 오라클(CPU)과 WSL(GPU) 양쪽 엔진을 **하나의 풀로 묶어** 쓸 수 있다.

### 노출 방식을 가정하지 않는다 (핵심 원칙)

엔진에 어떻게 닿을지는 **설정이 정한다.** 코드는 어떤 방식도 전제하지 않는다.

| 노출 방식 | 설정에 들어가는 값 |
|---|---|
| NPM + 서브도메인 | `https://stt.whisper.sinsan.kr` |
| 포트 직접 개방 | `http://203.0.113.9:8101` |
| 같은 서버 컨테이너 | `http://voice-whisper:8101` |
| 사설망 / VPN | `http://10.0.0.5:8101` |
| 유닉스 소켓·터널 등 | 해당 URL |

오케스트레이터 입장에서 이건 전부 **그냥 URL 문자열**이다. NPM 을 쓰는지, 포트를 여는지,
서브도메인 이름을 어떻게 짓는지 시스템은 알지도 못하고 알 필요도 없다.
따라서 서버마다 다른 방식을 써도 되고, 나중에 방식을 바꿔도 설정 한 줄만 고치면 된다.

**명명 규칙도 강제하지 않는다.** 뒤에 나오는 `stt.<엔진>.<도메인>` 같은 건 오라클에서 쓴 예시일 뿐,
시스템이 그 패턴으로 주소를 조립하지 않는다. 주어진 URL 을 그대로 쓴다.

### 레지스트리 정의

엔진 하나에 **엔드포인트를 여러 개** 달 수 있다. 개수도 고정이 아니다.
조건이 맞는 것 중 우선순위가 높은 것을 쓴다.

```yaml
engines:
  - id: whisper-large-gpu
    kind: stt                       # 종류는 이 메타데이터로만 판단한다. 포트로 추론하지 않는다
    modes: [realtime, ptt, batch]   # 지원 모드 (실측 성능으로 운영자가 정한다)
    streaming: true
    endpoints:                      # 필요한 만큼. 1개여도 되고 3개여도 된다
      - url: http://voice-whisper:8101
        when: { same_server: true } # 오케스트레이터가 같은 서버일 때만
        priority: 10
      - url: https://stt.whisper.example
        priority: 20                # 조건 없음 = 항상 후보
        api_key_env: ENGINE_KEY_WHISPER_GPU

  - id: supertonic-oci
    kind: tts
    modes: [ptt, batch]
    endpoints:
      - url: https://tts.supertonic.sinsan.kr
        api_key_env: ENGINE_KEY_SUPERTONIC_OCI
```

`when` 조건과 `priority` 만으로 "같은 서버면 내부 주소, 아니면 외부 주소"가 표현된다.
내부/외부라는 개념을 코드가 알 필요가 없다.

`modes` 는 **운영자가 실측 성능을 보고 정한다.** 시스템이 RTF 를 보고 자동 판단하지 않는다.
같은 whisper 라도 하드웨어에 따라 실시간 가능 여부가 달라지기 때문이다.
오케스트레이터는 요청 모드를 지원하지 않는 엔진이 선택되면 거부하고 이유를 돌려준다.

### 설정을 어디서 읽는가 — 이것도 고정하지 않는다

| 소스 | 용도 |
|---|---|
| 파일 (`engines.yaml`) | 기본. 볼륨 마운트로 주입 |
| 환경변수 | 컨테이너 오케스트레이션 환경에서 파일 없이 주입 |
| HTTP API (`/v1/admin/engines`) | 재기동 없이 런타임 추가·수정·제거 |

세 가지를 병행하고 우선순위는 **API > 환경변수 > 파일**. 파일은 변경 감지로 핫 리로드한다.
엔진을 하나 늘리려고 오케스트레이터를 다시 빌드하거나 재기동하는 일이 없어야 한다.

### 자동 가용성 파악

기동 시와 주기적으로 각 엔진의 `/health` 와 `/info` 를 호출해 상태를 갱신한다.
엔진이 꺼져 있으면(이 구성에서는 흔한 일이다 — 필요한 것만 띄우므로) 레지스트리에서
`unavailable` 로 표시하고 `GET /v1/config` 응답에서 제외한다. 클라이언트 설정 UI 는
**실제로 지금 쓸 수 있는 것만** 보게 된다.

`/info` 응답에는 이미 엔진명·모델·지원 언어·샘플레이트가 들어 있으므로 별도 정의가 필요 없다.

### 라우팅 정책

세션이 엔진을 명시하지 않으면 정책으로 고른다.

| 정책 | 동작 |
|---|---|
| `explicit` | 세션이 지정한 엔진만. 없으면 오류 |
| `mode_first` | 요청 모드를 지원하는 엔진 중 우선순위 순 (기본값) |
| `nearest` | 오케스트레이터와 같은 서버의 엔진 우선. 네트워크 왕복 제거 |
| `fallback` | 1순위가 unavailable 이면 다음 후보로. 배치에서만 권장 |

실시간에서 서버를 넘나드는 폴백은 지연이 튀어 오히려 나쁘다. 기본은 폴백 없이 오류 전달.

---

## 8. 컨테이너 구성과 배포 조합

엔진 레이어와 번역 애플리케이션은 **별도 폴더(별도 저장소)** 로 분리한다.
엔진은 여러 앱이 쓸 수 있는 인프라이고, 번역 앱은 그것을 소비하는 하나의 애플리케이션이다.
수명주기도 배포 대상도 다르다.

```
~/deploy/
├── voice/                   ── 엔진 레이어 (인프라)
│   ├── _common/voiceapi.py     엔진용 HTTP 규격
│   ├── _template/              새 엔진 스캐폴딩
│   ├── whisper/ moonshine/     STT 엔진
│   ├── supertonic/ melotts/    TTS 엔진
│   ├── ollama/                 로컬 LLM (ENGINE_KIND=llm)
│   └── voicectl.sh             엔진 제어
│
└── translate/               ── 번역 애플리케이션
    ├── orchestrator/           파이프라인 코어, 어댑터, WS/HTTP API
    ├── web/                    웹 클라이언트
    ├── config/                 설정 — 이 앱의 두뇌
    │   ├── engines.yaml           엔진 레지스트리
    │   ├── providers.yaml         LLM 프로바이더
    │   ├── profiles.yaml          세션 프로필 (단방향/양방향 등)
    │   ├── defaults.yaml          기본값. 코드에 두지 않는다
    │   └── secrets.env            비밀값 (버전관리 제외)
    ├── translatectl.sh         앱 제어
    └── DESIGN.md
```

`ollama` 를 `voice/` 에 두는 것은 그것이 **엔진 규약(engine.env + voicectl 관리)을 따르기 때문**이다.
음성이 아니라는 점에서 폴더 이름과 어긋나지만, 관리 방식이 같은 쪽에 두는 편이 실용적이다.

두 폴더는 같은 `proxy-network` 에 붙어 컨테이너 이름으로 서로 부른다.
**번역 앱은 엔진의 HTTP API 만 쓴다.** `voice/` 의 코드를 import 하지 않으므로 결합이 없고,
엔진 레이어를 통째로 다른 구현으로 바꿔도 번역 앱은 설정만 바뀐다.

### 배포는 설정으로 결정된다

오케스트레이터가 엔진을 URL 로만 다루므로, "무엇을 어디에 올릴지"는 코드가 아니라
`config/` 가 정한다. 같은 이미지로 모든 조합이 나온다.

```bash
# 엔진 (voice/)
cd ~/deploy/voice     && ./voicectl.sh start whisper supertonic

# 번역 앱 (translate/)
cd ~/deploy/translate && ./translatectl.sh start orchestrator web
```

### 서버 간 도달성

엔진 주소를 설정에서 URL 로 받으므로 **도달 방식은 시스템의 관심사가 아니다.**
NPM+서브도메인이든 포트 직접이든 사설망이든, 서버마다 다르게 써도 된다 (7장 참고).

따라서 **오케스트레이터 배치는 자유다.** 도달성이 아니라 실제 기준으로만 고른다.

| 기준 | 판단 |
|---|---|
| 지연 | 무거운 엔진과 같은 서버에 두면 네트워크 왕복이 사라진다. 실시간에서 유리 |
| 가동률 | 상시 켜져 있어야 하는지, 개발 중에만 쓰는지 |
| 외부 창구 | 이미 프록시·TLS 가 갖춰진 쪽 |

### 엔진을 외부에 노출할 때

인터넷에 열리는 순간 **엔진 `API_KEY` 는 선택이 아니라 필수다.**
오라클 엔진 4개는 이미 엔진별 키가 적용돼 있다.

노출 방식에 따라 겹쳐 걸 수 있는 수단이 다르다. 어느 것을 쓸지도 설정의 영역이다.

| 방식 | 추가로 걸 수 있는 것 |
|---|---|
| 프록시 경유 | Access List(IP 허용목록), basic auth, rate limit |
| 포트 직접 | 방화벽 / 보안그룹 IP 제한 |
| 사설망 | 네트워크 자체가 경계 |

호출자가 오케스트레이터 한 곳뿐이라 IP 제한이 특히 잘 맞는다.

---

## 9. 개발 환경과 운영 환경

지금의 오라클 ARM 서버와 WSL 데스크톱은 **개발 환경**이다. 운영은 용도에 맞는 서버를 따로 구축한다는
전제로 설계한다. 그래서 다음 두 가지를 지킨다.

**① 현재 하드웨어 제약을 설계에 박지 않는다**

"오라클 CPU 에서는 실시간이 안 된다"는 지금 하드웨어의 사실일 뿐 시스템의 성질이 아니다.
그래서 코드에 모드 제한을 하드코딩하지 않고, **엔진의 실측 RTF 를 레지스트리 메타데이터로 두고**
오케스트레이터가 판단하게 한다. 운영 서버에서 같은 whisper 가 RTF 0.1 로 돌면
`modes` 에 `realtime` 을 넣기만 하면 된다. 코드는 그대로다.

마찬가지로 CPU 전용 최적화(int8 강제, 스레드 수 고정 같은 것)를 코드에 넣지 않는다.
전부 `engine.env` 값이다.

**② 환경 차이는 설정 파일로만 드러난다**

| 계층 | 개발 | 운영 |
|---|---|---|
| 엔진 목록 | `engines.dev.yaml` | `engines.prod.yaml` |
| 비밀값 | `.env` 파일 (권한 600) | 시크릿 매니저 / 환경변수 주입 |
| 엔진 주소 | 공개 도메인 + 컨테이너 이름 혼재 | 사설망 내부 주소로 통일 |
| TLS | NPM + Let's Encrypt | 로드밸런서 종단 |
| 관측 | 컨테이너 로그 | 메트릭 수집 + 추적 |

이미지·코드는 동일하고 `CONFIG_PROFILE` 로 고른다.

### 운영 전환 시 추가로 필요한 것 (지금은 만들지 않되 막지 않는다)

- **WebSocket 세션 어피니티** — 오케스트레이터를 여러 대로 늘리면 같은 세션이 같은 인스턴스로
  가야 한다. 세션 상태를 인스턴스 메모리에만 두지 말고 세션 ID 를 외부에서 식별 가능하게 설계
- **수평 확장** — 오케스트레이터는 상태를 최소로 유지해 늘리기 쉽게. 무거운 건 엔진 쪽이므로
  엔진별로 독립적으로 증설되어야 한다 (지금 구조가 이미 그렇다)
- **관측** — 단계별 지연(`stt_ms`, `llm_ttft_ms`, `tts_ttfa_ms`)을 이미 WS 이벤트로 내보내므로
  그대로 메트릭으로 뽑을 수 있게 구조화 로그로 남긴다
- **비용 추적** — LLM 프로바이더별 토큰 사용량을 세션 단위로 집계
- **백프레셔** — 동시 세션이 엔진 처리량을 넘을 때의 큐잉·거절 정책

---

## 10. 클라이언트 3종

셋 다 **같은 API** 를 쓴다. 웹이 레퍼런스 구현이 된다.

**① 웹 브라우저** (`web/` 컨테이너, :8402)
- 마이크 캡처(AudioWorklet) → 16kHz mono PCM → WebSocket 바이너리 프레임
- 원문/번역문 실시간 표시, 오디오 큐 재생
- 설정 UI 는 `GET /v1/config` 응답으로 동적 생성
- **HTTPS 필수** — 브라우저가 HTTP 에서는 마이크 권한을 안 준다. NPM 으로 붙인다

**② API 직접 호출**
- WebSocket 규격 + HTTP 규격을 문서화
- `curl` 로 되는 배치 엔드포인트와 Python 예제 제공

**③ 데스크톱/모바일 앱**
- 같은 WS API 를 쓰는 별도 앱. 웹 클라이언트가 동작한 뒤 착수한다
- 앱의 이점은 AEC(에코 제거)와 백그라운드 동작 — `full_duplex` 를 쓸 수 있는 유일한 경로

---

## 11. API 규격 (초안)

### WebSocket `/v1/stream`

클라이언트 → 서버
```jsonc
{"type":"config", ...세션 설정...}          // 최초 1회
<바이너리 프레임>                            // PCM16 16kHz mono, 20ms 단위
{"type":"control", "action":"flush"}        // 강제 세그먼트 확정 (PTT 버튼 뗌)
{"type":"control", "action":"cancel"}       // 진행 중 번역 취소
```

서버 → 클라이언트
```jsonc
{"type":"ready",      "session_id":"...", "participants":[...]}
{"type":"vad",        "state":"speech_start"|"speech_end"}
{"type":"stt.partial","from":"a", "text":"안녕하세"}              // 표시 전용
{"type":"stt.final",  "seg":1, "from":"a", "lang":"ko", "text":"안녕하세요."}
{"type":"llm.delta",  "seg":1, "to":"b", "text":"Hel"}
{"type":"llm.final",  "seg":1, "to":"b", "lang":"en", "text":"Hello."}
{"type":"tts.chunk",  "seg":1, "to":"b", "seq":0, "sr":24000}     // 직후 바이너리 프레임
{"type":"tts.done",   "seg":1, "to":"b"}
{"type":"metrics",    "seg":1, "stt_ms":620, "llm_ttft_ms":310, "tts_ttfa_ms":540}
{"type":"error",      "code":"...", "message":"..."}
```

**모든 이벤트에 `from`/`to` 참여자 id 가 붙는다.** 단방향에서도 마찬가지다.
이 필드가 처음부터 있어야 양방향으로 갈 때 프로토콜을 깨지 않는다.
다중 기기 토폴로지에서는 클라이언트가 자기 `to` 인 오디오만 재생하면 된다.

오디오는 base64 가 아니라 **바이너리 프레임**으로 보낸다 (base64 는 33% 오버헤드).

### HTTP (ptt / batch)

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/v1/translate/audio` | 오디오 업로드 → 번역 음성 + 원문/번역문 |
| POST | `/v1/translate/text` | 텍스트만 번역 (STT/TTS 생략) |
| GET | `/v1/config` | 사용 가능한 엔진·프로바이더·모드 |
| GET | `/health` | 상태 (인증 불필요) |

인증은 엔진과 동일하게 `API_KEY` + `Authorization: Bearer`.

---

## 12. 결정이 필요한 것

구현 전에 정해야 할 것들. 지금 답이 없어도 되지만 순서상 막히는 지점이다.

1. **VAD 를 어디서 돌릴 것인가** — 클라이언트(브라우저)냐 서버냐.
   클라이언트면 대역폭이 크게 줄고(무음 안 보냄) 서버 부하도 준다. 서버면 클라이언트가 단순해지고
   정책을 한 곳에서 관리한다. **서버 권장** — 클라이언트 3종을 각각 구현해야 하므로

2. **스트리밍 STT 구현체** — WhisperLive(WebSocket 서버/클라이언트 구조) vs whisper_streaming(라이브러리).
   전자는 별도 서버라 우리 규격에 맞추는 어댑터가 필요하고, 후자는 우리가 직접 감싼다.
   **후자 권장** — `_common/voiceapi.py` 규격 안으로 들어와 일관성이 유지된다

3. **대화 맥락 유지 범위** — 몇 턴을 컨텍스트로 넣을지. 길수록 정확하고 느리고 비싸다

4. **양방향의 토폴로지** — 단일 기기(한 마이크를 공유)로 갈지 다중 기기(각자 클라이언트)로 갈지.
   단일 기기가 확장 경로상 먼저다. 2단계 착수 전에 정하면 된다

---

## 13. 개발 단계

**설계는 최종 목표(실시간 양방향)에 맞춰 지금 한 번에 하고, 개발만 두 단계로 나눈다.**
1단계에서 만든 것을 2단계에서 뜯어고치지 않는 것이 이 구분의 목적이다.

### 1단계에서 반드시 지킬 불변식

1단계는 단방향·배치/PTT 로 좁히되, 아래를 어기면 2단계가 재작성이 된다.
**이것들은 "나중에 하면 되는 것"이 아니라 처음부터 그렇게 만들어야 하는 것이다.**

| 불변식 | 어겼을 때 |
|---|---|
| 세션은 **참여자 목록**을 갖는다. 단방향은 한쪽 `input: false` 일 뿐 | 양방향에서 세션 모델을 다시 짬 |
| 모든 이벤트에 `from` / `to` 참여자 id 가 붙는다 | 프로토콜 파괴적 변경 → 클라이언트 3종 전부 수정 |
| 번역 방향은 **발화자 언어 → 수신자 언어**로 계산한다. `ko→en` 같은 상수가 없다 | 언어쌍이 코드에 스며듦 |
| 화자 식별은 어댑터. 1단계는 `manual`(고정) 하나만 등록 | 언어 감지 추가 시 파이프라인 분해 |
| 파이프라인은 **세그먼트 단위**로 돈다. "발화 전체가 한 번에 온다"를 가정하지 않는다 | 스트리밍 전환 시 코어 재작성 |
| 엔진 어댑터 인터페이스에 스트리밍 경로를 열어둔다 (1단계는 배치만 구현) | 어댑터 계층 전체 수정 |
| 모드·정책은 레지스트리 등록. 1단계는 `batch`·`ptt` 만 등록 | 열거형이 코드에 굳음 |

즉 **1단계는 기능을 줄이는 것이지 구조를 줄이는 것이 아니다.**

### 1단계 — 단방향, 배치/PTT (오라클 서버에서 지금 가능)

각 항목이 끝날 때마다 동작하는 것이 나온다.

1. **오케스트레이터 골격** — 설정 로더(파일/환경변수/API), 레지스트리, 어댑터 해석기.
   여기서 "아무것도 고정하지 않는다"가 실제로 지켜지는지 판가름난다
2. **배치 파이프라인** — `POST /v1/translate/audio` 로 STT→LLM→TTS. 기존 오라클 엔진으로 검증
3. **LLM 프로바이더 연결** — 어댑터 계열 3종, 프로바이더 설정 체계, `GET /v1/config`
4. **웹 클라이언트 (PTT)** — 마이크 녹음 → 업로드 → 재생. 전체 흐름이 눈에 보인다
5. **WebSocket + VAD + 세그먼트 파이프라인** — 프로토콜 확정, 턴 관리 골격.
   아직 단방향이지만 `from`/`to` 는 이미 흐른다

### 2단계 — 실시간 + 양방향 (GPU 서버 필요)

6. **스트리밍 STT 엔진** — 실시간의 전제. 어댑터의 스트리밍 경로를 채운다
7. **스트리밍 TTS 청크 파이프라인** — TTFA 최적화
8. **실시간 모드** — 지연 실측, 병목 조정. 목표 TTFA 대비 검증
9. **양방향** — 화자 식별 어댑터 추가(`language_detect`), 참여자 둘 다 `input: true`.
   불변식을 지켰다면 여기서 **설정 변경 + 어댑터 하나 추가**로 끝난다
10. **다중 기기 토폴로지** — 세션 합류, 참여자별 오디오 분리, `full_duplex`
11. **데스크톱/모바일 앱** — AEC 를 쓸 수 있는 유일한 경로

### 단계 경계에서 확인할 것

1단계를 끝낼 때 **`twoway` 프로필로 세션을 열어보고 어디서 막히는지 확인한다.**

기대하는 결과는 딱 하나다 — `/v1/config` 에서 `twoway` 가 "`language_detect` 어댑터 없음"으로
사용 불가 표시되고, 강제로 열면 그 이유로 거부당하는 것. **그 외에는 아무것도 깨지지 않아야 한다.**
세션 모델·프로토콜·파이프라인·라우팅이 함께 무너지면 불변식을 어긴 것이므로
2단계로 넘어가기 전에 고친다.
