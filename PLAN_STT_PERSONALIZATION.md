# 작업 계획 — STT 개인화 (사용자별 음성인식 개선)

설계는 `DESIGN.md` §16을 따른다. **이 문서는 계획서다 — 아직 개발 승인 전이다.**
사용자의 명시적 "진행해" 없이는 어떤 단계도 착수하지 않는다.

담당: 서버 = 이 세션, 앱 = "번역 앱 작업"(`translate-54`) 세션.

목표 재확인: **모든 사용자의 STT 개선이 아니라, 등록해서 자기 음성 데이터를 제공한 사람
한 명만을 위한 개선.** 방법 B(LLM 문맥 활용)를 먼저 하고, 방법 A(실제 파인튜닝)는 장기 과제.

---

## 0단계 — 데이터 수집 기반 (`modules/stt_training`)

방법 A·B 둘 다의 재료. 이게 없으면 아무 것도 시작 못 한다.

### 서버 (0-S)

| # | 작업 | 내용 |
|---|---|---|
| 0-S1 | 설정 스키마 | `config/defaults.yaml`에 `stt_training:` 섹션 — 언어별 문장 은행(한국어/영어 최소), 목표 횟수(`required_read_count`, `required_verify_count`), 저장 경로, 보존 정책(판정 안 된 verify 임시 샘플 만료 기간 포함) |
| 0-S2 | 저장소 2종 | 낭독교정 저장소(문장+음성 확정쌍), 정오판정 저장소(음성+STT결과+판정+교정텍스트, verify 요청 시 임시저장 → verdict로 확정) — `lang_learn`의 `history_store.py`와 같은 패턴(JSON+락+원자적쓰기) |
| 0-S3 | 낭독 교정 API | `GET /v1/users/{user_id}/stt_training/next_prompt`(아직 안 읽은 문장, 목표 채우면 "다 했다" 응답) / `POST .../read_sample`(오디오+prompt_id 업로드 → 저장, 진행개수 반환) |
| 0-S4 | 정오 판정 API | `POST .../verify`(오디오 업로드 → STT 실행 → 인식결과+sample_id 반환, 임시저장) / `POST .../verify/{sample_id}/verdict`(`correct:true` 또는 `correct:false`+`corrected_text` 필수 → 확정저장. `corrected_text` 없이 `false`면 400) |
| 0-S5 | 진행 상황 API | `GET .../status` → `{"read":{"done","required"}, "verify":{"done","required"}}` |
| 0-S6 | 모듈 등록 | `@module("stt_training")`, `/v1/config`에 언어 목록·목표 개수 노출(하드코딩 금지) |
| 0-S7 | 검증 | 기존 기능(화자등록/사용자/lang_learn) 회귀 확인, 낭독·정오판정 양쪽 왕복 curl 검증(교정텍스트 누락 400 케이스 포함) |

### 앱 (0-A) — 앱 담당자에게 전달할 내용

| # | 작업 | 내용 |
|---|---|---|
| 0-A1 | 온보딩 진입점 | 최초 로그인 후(또는 설정 화면에서) "STT 학습" 유도 화면 — `GET .../status`로 아직 안 끝났으면 노출 |
| 0-A2 | 낭독 교정 화면 | `next_prompt`로 문장 받아 화면에 표시 → 마이크로 읽은 음성 녹음 → `read_sample` 업로드 → 남은 개수 갱신, 목표 채우면 완료 표시 |
| 0-A3 | 정오 판정 화면 | 자유발화 녹음 → `verify` 업로드 → 인식 결과 텍스트 화면에 표시 → 사용자가 "맞음"/"틀림" 선택 → 틀림이면 정답 텍스트 입력창 노출(필수) → `verdict` 전송 |
| 0-A4 | 진행 표시 | 두 항목 각각 진행률(done/required) 표시 |

---

## 1단계 — 방법 B: LLM 문맥 활용 개인화 (우선 과제)

0단계 데이터가 어느 정도 쌓인 사용자부터 적용.

### 서버 (1-S)

| # | 작업 | 내용 |
|---|---|---|
| 1-S1 | 개인화 힌트 생성 | 0단계에서 모은 (음성,정답텍스트) 확정쌍을 사용자별로 모아 LLM 프롬프트용 힌트로 가공(예시 몇 개 선별, 또는 자주 틀리는 단어 목록 추출) |
| 1-S2 | `audio_direct` 어댑터 | `DESIGN.md` §5의 미착수 아이디어를 실제 구현 — 오디오를 멀티모달 LLM(Gemini/OpenAI/오디오지원 OpenRouter 모델)에 직접 보내 텍스트로 받음. 프롬프트에 1-S1의 힌트 포함 |
| 1-S3 | 라우팅 | `core/speech.to_text()`에서 세션의 `user_id`가 개인화 대상(힌트 존재)이면 `audio_direct`로, 아니면 기존 엔진 그대로(하위호환) |
| 1-S4 | 모드 제한 | 오디오 토큰 비용·지연 때문에 배치/PTT 모드부터 적용, 실시간(WS)은 나중에 재검토 |
| 1-S5 | 설정 | 오디오직결에 쓸 프로바이더/모델을 설정으로(하드코딩 금지), 프로바이더가 오디오를 못 받으면 조용히 폴백하지 않고 명시적 오류(§5 기존 원칙과 동일) |
| 1-S6 | 검증 | 개인화 대상 사용자 vs 일반 사용자 각각 STT 결과 비교, 비용/지연 실측 |

### 앱 (1-A) — 앱 담당자에게 전달할 내용

| # | 작업 | 내용 |
|---|---|---|
| 1-A1 | (선택) 상태 표시 | "개인화 STT 적용 중" 같은 표시가 있으면 좋지만 필수는 아님 — 서버가 알아서 라우팅하므로 앱 쪽 필수 작업은 없음 |

---

## 2단계 — 방법 A: 실제 파인튜닝 (장기 과제, 설계만 확정)

**착수 시점 미정.** 방법 B 효과를 보고 판단.

### 서버 (2-S, `translate` 범위)

| # | 작업 | 내용 |
|---|---|---|
| 2-S1 | 데이터 내보내기 | `GET /v1/users/{user_id}/stt_training/export` — 확정 샘플 매니페스트(텍스트+오디오 다운로드 링크) |
| 2-S2 | 준비 상태 신호 | `GET .../status`에 `ready_for_training: bool` 추가 |
| 2-S3 | 사용자별 엔진 라우팅 | 신규 `core/user_engines.py` — `{user_id: {kind: engine_endpoint_override}}`, `PUT /v1/users/{user_id}/engine_overrides`로 등록. override 없으면 하위호환 |

### voice 저장소 (또는 외부 임대 서버) 쪽 — `translate` 범위 아님

- 실제 GPU/외부 서버에서 학습 코드 실행
- 학습된 모델 저장·버전 관리
- 모델을 추론 서버로 서빙

**누가 이 부분을 할지는 아직 미정** — voice 저장소 담당자와 별도로 조율 필요.

---

## 순서 요약

```
0단계 (서버+앱, 데이터 수집) → 1단계 (서버 위주, LLM 개인화) → 관찰 →
2단계 착수 여부 판단 (서버 쪽 export/라우팅 설계는 미리 되어 있음, voice쪽 조율은 별도)
```

각 단계 착수 전 반드시 사용자 승인을 받는다.
