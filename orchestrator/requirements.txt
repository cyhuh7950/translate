# 오케스트레이터 의존성
#
# 엔진과 달리 추론 라이브러리가 없다. 순수 파이썬이라 아키텍처를 가리지 않는다
# (개발용 ARM64 오라클, 운영용 x86_64 어디서든 같은 이미지 정의로 빌드된다).

fastapi>=0.115,<1
uvicorn[standard]>=0.30
python-multipart>=0.0.9
httpx>=0.27
pyyaml>=6.0
numpy>=1.24,<3
